// 3.4 v

const name = "Carlos Franco";
const skate = "Fliping Wood"

function toUpper(text) {
    const upperCased = text.toUpperCase();
    console.log(upperCased);
}

const toUpper = () =>{
    hello
}



const location {
    country: US, state:TX, city: SA
}

// the methods were a bit of confussing for me 

<button onmouseover="this.innerHTML =Date()">The time</button>
